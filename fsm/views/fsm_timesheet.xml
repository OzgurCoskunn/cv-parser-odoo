<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="tree_timesheet" model="ir.ui.view">
    <field name="name">fsm.timesheet.list</field>
    <field name="model">account.analytic.line</field>
    <field name="arch" type="xml">
        <list string="Timesheets">
            <field name="date" decoration-bf="1"/>
            <field name="fsm_date_from" string="Start Date"/>
            <field name="fsm_date_to" string="End Date"/>
            <field name="account_id" options="{'no_create': True}" optional="hide"/>
            <field name="fsm_project_id" options="{'no_create': True}" string="Project"/>
            <field name="fsm_task_id" options="{'no_create': True}" string="Task"/>
            <field name="name" string="Stage" widget="badge"/>
            <field name="user_id" widget="many2one_avatar_user" options="{'no_create': True}"/>
            <field name="unit_amount" optional="show" widget="timesheet_uom" sum="Total" decoration-danger="unit_amount &gt; 24 or unit_amount &lt; 0"/>
            <field name="company_id" invisible="1"/>
        </list>
    </field>
    </record>

    <record id="action_timesheet" model="ir.actions.act_window">
    <field name="name">Timesheets</field>
    <field name="type">ir.actions.act_window</field>
    <field name="res_model">account.analytic.line</field>
    <field name="view_mode">list,kanban,pivot,graph</field>
    <field name="domain">[('fsm_task_id', '!=', False)]</field>
    <field name="context">{'create': False, 'edit': False, 'delete': False}</field>
    <field name="view_ids" eval="[(5, 0, 0),
        (0, 0, {'view_mode': 'list', 'view_id': ref('tree_timesheet')})]"
    />
    </record>
</odoo>